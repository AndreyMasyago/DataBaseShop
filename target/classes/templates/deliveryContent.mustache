<!DOCTYPE HTML>
<html>
<head>
    <title>deliveryContent so good</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
</head>
<body>
<h1>
    <div>
        <form method="post" action="/insert/deliveryContent">
            <select name = "deliveryId" placeholder = "Деливери">
                {{#deliveries}}
                    <option value = {{deliveryId}}>
                        {{deliveryId}} {{arrivingDateOnStorage}}
                    </option>
                {{/deliveries}}
            </select>

            <input type="number" name="amount" min="1"placeholder="Количество товара"/>

            <select name = "goodsId" placeholder = "Гудсик">
                {{#goods}}
                    <option value = {{goodsId}}>
                        {{goodsName}} {{detailId}} {{size}} {{deliveryTime}}
                        {{purchasePrice}} {{sellingPrice}} {{producer}} {{providerName}}
                {{/goods}}
            </select>
            <button type="submit">ДОБАВЬ МЕНЯ СЮДААА</button>
        </form>
    </div>
    {{#deliveryContent}}
        <div>{{arrivingDateOnStorage}} {{amount}} {{goodsName}}</div>
    {{/deliveryContent}}
</h1>
</body>
</html>